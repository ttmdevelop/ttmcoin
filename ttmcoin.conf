# Generated by ttm develop team 
# This config should be placed in following path:
# ~/.ttm/ttmcoin.conf

daemon=0
#maxconnections=20

# 启动节点，修改路径
datadir=/root/ttmnode

# 设定默认为私有链
regtest=0
testnet=0

#enable mining
gen=0

# [rpc]
# Accept command line and JSON-RPC commands.
server=1
txindex=1

# Username for JSON-RPC connections
rpcuser=ttmrpc
# Password for JSON-RPC connections
rpcpassword=lQ1Pn9RWMm2ABKNteFFQEoDNStHyVBm2g1VlFL1SayMJbX
# Listen for JSON-RPC connections on this port
rpcport=18332

#rpcbind=172.16.0.98

# Allow JSON-RPC connections from specified source. Valid for <ip> are a single IP (e.g. 1.2.3.4), 
# a network/netmask (e.g. 1.2.3.4/255.255.255.0) or a network/CIDR (e.g. 1.2.3.4/24). This option 
# can be specified multiple times.
rpcallowip=0.0.0.0/0


# Use as many addnode= settings as you like to connect to specific peers
#addnode=69.164.218.197
#addnode=10.0.0.2:29333
addnode=127.0.0.1:9998
